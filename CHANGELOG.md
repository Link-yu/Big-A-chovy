# 更新日志

本文件记录源码、工具链和使用方式的主要变化。所有版本目前均为预览版，不代表稳定版或收益保证。

## v0.4.1-preview.1 — 2026-09-21

### 新增

- **资金约束模拟盘**（10 万本金 · 规则自动裁决 · 考核净值曲线）：与既有「信号样本采集模拟仓」**职责隔离**——后者资金与笔数不限（框架第 69/104 行），本盘固定本金、逐日结算净值。
  - 引擎 `tools/sim_account.py`：`status` / `scan` / `open` / `check-exit` / `close` / `settle` / `nav` / `sync` / `auto`。**全部规则判定在脚本内完成**，定时任务只负责触发与落盘，避免语言模型侧自由裁量导致结果不可复现。
  - 账本 `tools/sim_data/sim_account.json`（私有，不入 GitHub），并自动把持仓快照与成交流水同步到 `决策记录/YYYYMMDD.md`（复用既有 `positions.simulated` 段与新增 `cash.sim_available`）。
  - 仓位口径：1 手起步、单股上限 1/4、单日最多新开 5 只；档位 S 固定 1 手 / A 1/40 / B 1/10；13:00–13:45 午后回流手数减半；跨周目标仓位减半。
  - 买入门槛：正式全过 **或** 框架已许可实验类别（回落放宽／coalition／观察池突破／sector_boost），逐笔标注类别与真实仓卡点；公告 `avoid/unknown` 与超大单为负在模拟盘同样否决。
  - 离场与真实仓同标准：止损 −3.5%、止盈 +2%、T+1 当日不可卖、次日 09:45 常规退出截止。
  - 登记：`tools/rule_config.py` 新增 `sim` 段；《选股框架.md》第六章与参数总表登记为**待验证**项，明确**不参与真实仓放行**。
  - 操作手册：`docs/模拟盘操作手册.md`。

### 说明

- 模拟盘**不模拟滑点、手续费与涨跌停无法成交**，收益曲线天然偏乐观，仅用于横向比较规则优劣，不得当作真实收益预期。
- 档位阈值（主力≥10%、5 分增量≥1000 万、板块内候选≥2）与「1 手起步」口径为新增待验证项，满 20 个完整结算样本后再评估转正。

## v0.4.0-preview.2 — 2026-09-21

### 修复

- **修复「看板午后停止出实时数据」**：根因是本机代理出口而非网络变慢。`proxy_ports.json` 的候选端口里有 Clash Verge 的 7897，其出口为日本东京；东财对海外出口拦截 `push2`/`push2his`（该节点下后者 100% 失败），而探测端点的排序惩罚被同质化，导致该节点反而胜出、引擎全程走海外出口，单轮跑满 `SCREENING_TIMEOUT` 超时、看板退化为反复重放旧快照。**候选端口最终清空为 `[]`（直连实测可用，本机不需要任何代理）**，并在配置内注明禁止加回海外端口。
- 修复 `load_candidate_ports` 的空数组语义：显式写 `"candidate_ports": []` 原会被当作「读不到配置」而回落到默认 `[7890, 7897]`——即「想禁用代理」被静默翻译成「启用代理」。

### 二次修正

- 曾把端口 `1030` 写入候选（其出口为杭州电信、对东财无害），后查实它属 `sandbox-cli`——**WorkBuddy 沙箱会话的本地代理，并非本机常驻服务**，随会话生死。保留会让「WorkBuddy 会话内」与「用户自建终端」选到不同路径、行为不可预测，故一并移除。新增规则：候选只收「本机常驻 + 国内出口」的端口。
- `test_network_path.py` 的 `test_shipped_config_is_valid` 原断言「自带配置必须解析出端口」，与「空数组 = 禁用代理」冲突；改为校验配置可被显式解析，且仅在非空时要求有端口。

### 变更

- `PROBE_ENDPOINTS` 重构为四元组 `(名称, URL, 参数, 采样次数)`：移除在所有出口均已失效的 `82.push2`（引擎资金流 `f62/f184` 实取自 `push2delay` 的 clist 接口，不依赖该域名；全员失败的端点只会给每条路径加同样惩罚，掩盖真实差异），主端点保留 `push2delay`，K线端点 `push2his` 采样 3 次取最快成功值以抵御约 1/3 概率的间歇断连。
- `DEGRADE_PENALTY_MS` 1000 → 3000：端点残缺在批量阶段造成的损失远超 1s，原值让「延迟低但端点残缺」的路径压过「端点全通」的路径。
- 看板默认 `network_mode` 由 `auto` 改为 `direct`（本机实测纯直连一轮 49s、零降级；`auto` 会选到代理而拉长到 76s+）。
- `SCREENING_TIMEOUT` 120 → 180：本机东财接口实测 450~820ms，盘中一轮约 90~100s，原余量仅 20s，偶发抖动即误判超时。

### 验证

- `test_network_path.py` 29 用例通过（新增端点表结构用例，同步三元组改动）。
- 实地对比：纯直连一轮 49.4s / `market_fetch_complete=true` / 零降级；修复前 `auto` 模式 120s 超时。

## v0.4.0-docker.1 — 2026-09-21

- 增加面向公开 GitHub 仓库的 Docker 看板部署文件、运行状态卷和 Docker 构建校验 workflow。
- 通过 `.env.example` 配置容器代理；报告、决策记录、影子样本和运行缓存继续保持本机私有。
- 发布 `linux/amd64` 与 `linux/arm64` 多架构镜像到 GitHub Container Registry。

## v0.4.0-preview.1 — 2026-09-10

当前开发预览版。

### 主要变化

- 网络路径改为代码内实测择优：并发测试直连、本机候选代理端口、环境代理和 macOS 系统代理，按真实可用延迟选择路径；候选端口统一由 `daily-stock-analysis/scripts/proxy_ports.json` 配置。
- 修复 GUI、实时看板和 `.command` 启动器中的本机绝对路径，输出目录按项目根目录解析，克隆到其他位置后可运行。
- 将 `筛选结果/`、`决策记录/`、持仓、影子样本、运行缓存、日志和旧备份加入共享 `.gitignore`，避免个人运行数据进入 GitHub。
- 增加项目级 `AGENTS.md`、盘中 Skill 入口、ggp 显示/机器输出契约，以及对应的渲染和校验工具。
- 统一规则配置、公告风险状态、观察池状态机和一致性检查，保留“发现工具不负责最终交易裁决”的边界。
- README、CLAUDE 和启动器统一说明代理配置、版本通道及 Windows 尚未适配的限制。

### 验证

- 113 项单元测试通过。
- 一致性检查 26 PASS、0 FAIL、0 WARN。
- Python 语法、Shell 语法和 Git diff 检查通过。

### 已知限制

- 当前仍以 macOS 为主要支持环境，未完成 Windows 适配。
- 盘中网络稳定性和策略效果仍需继续积累样本验证。
- 工具只负责筛选、证据发现和数据查询，不自动下单，也不替代人工交易裁决。

## v0.3.5-preview.1 — 2026-08-30

线上预览基线版本。

- 整理盘中 Skill、筛选引擎、规则配置和一致性检查工具。
- 增加基本面查询、影子验证和报告审计相关能力。
- 记录网络代理依赖、保存路径和 Windows 适配等后续问题，作为 v0.4.0-preview.1 的对照基线。

## v0.3.4 — 2026-08-24

历史版本，保留原有标签；该版本开始明确记录 Windows 尚未适配的限制。
